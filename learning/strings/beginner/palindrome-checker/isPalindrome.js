/*
Problem: isPalindrome(str)

Goal:
Return true if the string reads the same forward and backward.

Sample Input:
isPalindrome("madam")

Sample Output:
true

Concepts Used:
- Strings
- Two-pointer logic
- Loops
- Comparisons
- Input validation

Rules:
- Use CommonJS syntax
- Keep the function body empty for manual practice
- Throw TypeError for non-string input
*/

function isPalindrome(str) {
}

module.exports = isPalindrome;
