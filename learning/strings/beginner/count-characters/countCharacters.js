/*
Problem: countCharacters(str)

Goal:
Return the number of characters in a string by counting manually.
Do not use .length.

Sample Input:
countCharacters("mahesh")

Sample Output:
6

Concepts Used:
- String traversal
- Loops
- Manual counting
- Input validation

Rules:
- Do not use .length
- Loop through the string and count each character manually
- If the input is not a string, throw a TypeError
*/

function countCharacters(str) {
}

module.exports = countCharacters;
