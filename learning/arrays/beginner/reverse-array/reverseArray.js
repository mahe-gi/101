/*
Problem: reverseArray(arr)

Goal:
Return a new array with the values in reverse order.
Do not use reverse().

Sample Input:
reverseArray([1, 2, 3, 4])

Sample Output:
[4, 3, 2, 1]

Concepts Used:
- Arrays
- Loops
- Indexing
- Function basics
- Input validation

Rules:
- Use CommonJS syntax
- Keep the function body empty for manual practice
- Throw TypeError for non-array input
*/

function reverseArray(arr) {
}

module.exports = reverseArray;
